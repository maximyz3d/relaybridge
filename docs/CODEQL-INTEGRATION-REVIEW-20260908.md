# CodeQL integration review — September 8, 2026

Scope: [PR125](https://github.com/maximyz3d/relaybridge/pull/125), initial head `1a02be0aaab582ac1caf85278196e804c3c8b823`, main `5f55ad641ebc1d71fdf4ac2561cb487eed31810a`. The same source was first published through fork PR124. GitHub's [default setup excludes fork pull requests](https://docs.github.com/en/code-security/concepts/code-scanning/setup-types), so the identical commit was published on the root agent's own upstream feature branch. Both histories remain preserved.

[CodeQL analysis run 34183662174](https://github.com/maximyz3d/relaybridge/actions/runs/34183662174) completed, but [result check 101927829173](https://github.com/maximyz3d/relaybridge/runs/101927829173) **failed with 30 alerts: one critical, 28 high, one medium**. Analysis completion was not a passing security gate. The exact SARIF for analysis `1738640531` was read locally, including all source-to-sink flows. No raw SARIF, machine logs or private runtime data is included here.

Three independent read-only native reviewers partitioned all 30 alerts. Root reviewed their source-backed findings. The proposed dispositions below require the fresh Claude closing review and actual GitHub disposition/check readback; this document alone does not clear any alert. Main also reports 48 existing open alerts: this review is confined to the 30 surfaced on this PR, including the 12 shared alert IDs 10, 11, 14–20, 33, 48 and 74. It does not declare the entire repository free of vulnerabilities.

| Alerts | Actual SARIF source and validation | Proposed disposition |
| --- | --- | --- |
| 11, 74 | HTTP task ID at server5303/5309 passes queue `taskPath()` full-match `/^t_[A-Za-z0-9_]+$/` before fixed task-directory existence/read. Dots, separators and absolute names cannot pass. | False positive: validated basename. |
| 64, 65, 66, 67, 75 | The same task-ID source flows into globally modeled `fs` monkeypatch parameters in separate tests. Some traces chain wrappers from different tests. Production never imports these test modules; synchronous tests use private fixture roots and restore wrappers. | Used in tests: cross-test global modeling. |
| 68 | Validated collaboration-ID path flows through production rename into a test-only monkeypatch, not loaded by production. | Used in tests. |
| 16, 17, 18, 19 | Collaboration update ID is checked by `collabPath()` against `/^c_[a-z0-9]+_[a-z0-9]+$/` and exact dirname containment before UUID temporary publication beneath fixed COLLABS_DIR. | False positive: rejecting validation dominates publication. |
| 10, 14, 15, 63, 69 | Requested cwd is intentional input to allowed-root validation. Lexical containment, startup-pinned root identity, directory existence and canonical containment precede acceptance. One-shot revalidates before spawn. A metadata probe can follow a candidate symlink while rejecting it; it neither returns contents nor authorizes outside-root execution. | False positive for reported traversal. Not an OS sandbox or universal same-user race proof. |
| 20, 55, 56, 57, 58, 59, 60, 61, 62 | Actual source is broadcast **effort**, not caller-selected executable. `optionalEnum` restricts it to minimal/low/medium/high/xhigh/max. CodeQL loses argv position and propagates this argument into command element zero. Effort replacement preserves the executable; model insertion begins after index zero. For alert61, enum values contain no slash, so `resolveSlot` returns before the reported filesystem sink. Windows qualification additionally rejects unknown/indirect templates and interprets no shell. | False positive: enum validation plus argv-position conflation. Cursor enumeration is not claimed to be no-follow/bounded before final launch validation; that is separate from these traces. |
| 53, 54 | Actual source is onboarding repository **name**, validated as strict owner/repo (no dot/dotdot components), then converted into one leaf under a private mkdtemp root. Fixed template targets reject traversal; ancestor/leaf checks reject symlinks and nonregular/hardlinked leaves; existing bytes are read by immutable validated Git blob identity. | False positive for reported repository traversal. Not protection against every malicious same-user filesystem race. |
| 33 | `req.body.data` → existing `Session.write()` → terminal PTY input. REST requires capability token plus Host/Origin checks; WebSocket separately checks Host/Origin/token. Terminal tools are excluded from remote MCP. Authorized shell input is the explicitly documented feature, not unintended evaluation of passive data. | False positive for unauthorized injection. Capability possession grants powerful host execution and remains a trust boundary. |
| 48 | Provider tag key → `cfg[kind]` → tags assignment. The guard requires an AI provider (or exact powershell); `isAiProviderEntry` rejects every underscore-prefixed key, including `__proto__`. Constructor/toString fail the object-type check. | False positive for the reported prototype mutation path. |

All reported server sink functions and queue path validation predate this merge; the new helper/test alerts come from the preserved PR118 history. This fact alone is not grounds for dismissal: the table relies on the actual rejecting guards, source enums, intentional authorized operations and test-only traces.

No CodeQL rule, workflow, query suite, authentication requirement or runtime safeguard is disabled. Any confirmed false-positive/test-only disposition must be per alert, with its reason and this review linked. Full checks and an explicit completed Claude verdict remain required before merging. Shared bridge installation, restart and issue122 legacy recovery remain excluded.
