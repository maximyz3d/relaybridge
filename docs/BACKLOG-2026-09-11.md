# Maintenance acceptance: September 11, 2026

Base: `c42c65bb026a665b5bf4aadd18d77060b07c684e` (2.3.3).
The intake contained 23 open issues and no open pull requests. Historical closed
PRs were checked for superseding merges; they are not reopened. Resolved issues
are closed with evidence, preserving their discussion and reproduction history.

The earlier parallel-provider and usage-continuity changes are already on main
through PRs 129 and 130. This pass addresses remaining transport, provenance,
provider diagnostics and recovery gaps. Independent review and exact-commit CI
remain release gates; passing a fixture does not qualify an external provider.

## Disposition

| Issue | Implementation / evidence | Closure condition or remaining work |
| --- | --- | --- |
| 16 | Explicit workspace provenance; `workspace-grounding.test.js`, `grounding-contract.integration.test.js` | Grounded reviews require validated cwd or intact inline evidence. |
| 17 | Durable seat cooldown, corruption refusal, source receipt CAS, Gemini terminal observer; cooldown/quota tests | Cross-provider native PTY qualification remains narrower than pipe parsing; do not infer percentage from qualitative denial. |
| 19 | Persistent `mcp/launcher.mjs`, per-host replaceable adapter; launcher lifecycle, SDK, installer and build identity tests | Lost calls are never replayed. Incompatible replacement requires host reload. |
| 24 | Existing Claude/Codex restrictions and provider budgets retained; corrected `dontAsk` documentation | Gemini/Copilot/Cursor/Grok native no-write qualification remains open. |
| 70 | Already fixed on main; evidence attached to the issue | Closed during intake. |
| 75 | Oversize workflow input rejects before mutation; lossy artifacts cannot approve; bounded terminal delivery receipts | Keystroke transmission is not semantic prompt consumption. Unsupported bulk terminal prompts refuse. |
| 76 | Explicit Copilot writer authorization, installed-help-qualified controls, first denial stop; permission-policy tests | Vendor tool approval does not contain filesystem writes. Native scoped-writer qualification remains open. |
| 77 | Source-placement classifier regression with exact retained fixture; router tests | Existing medical, secret and destructive-action gates retained. |
| 78 | Finite native limit is disclosed; approaching limit persists a checkpoint and stops with a separate reason | Native print wait is not renewable. Indefinite transport and newer native warning schemas remain unsupported. |
| 79 | Already fixed on main; evidence attached to the issue | Closed during intake. |
| 80 | Exact local Ollama plan-to-ask budget regression | Null/unset/positive budget behavior and zero-call invalid budget proved. |
| 85 | Candidate staging and explicit unsupported-contract guard; 25 staging regressions | Real Linux containment fixtures and one native Claude staged-write fixture pass. General native provider enrollment remains unqualified; declared write-set requests still refuse. No promotion API. |
| 86 | Bounded descendant census, process-local keyed command hashes, sampled child/test policy, exact active cancellation | Census is an observation, never termination proof. Optional command prevention is not implemented. |
| 87 | Canonical/ambiguous/missing citation checks; known false citations make an audit unusable | Path existence does not prove correctness of file contents or an audit conclusion. |
| 93 | Authenticated bounded hosted model catalog; endpoint/key/generation cache binding; hosted catalog authority tests | Metadata discovery never generates an answer or authorizes an unavailable model. |
| 94 | Already fixed on main; evidence attached to the issue | Closed during intake. |
| 97 | Already fixed on main; evidence attached to the issue | Closed during intake. |
| 98 | Already fixed on main; evidence attached to the issue | Closed during intake. |
| 102 | Explicit answer-path probe, bounded CLI version and provider state, separate freshness/auth/quota; diagnostic tests | Native `pwm` unavailable here; upstream empty answer has unknown root cause. Probe cannot bypass safe-provider eligibility. |
| 104 | Reconstructible delivery receipt outbox, deterministic deduplication, explicit acknowledgement, cache-amplification advisory | Completion, persisted result and acknowledged delivery are separate facts; no provider replay. |
| 105 | Exact MCP holder census before staging and cutover; native Windows lifecycle tests | Native tests preserve live REST PID/identity and all token/data/tree bytes. Platform CI still required for release. |
| 112 | Enrollment software already fixed | Shared live runtime is unenrolled. No runtime/config migration is authorized by this maintenance plan. |
| 122 | Private journal, queue, workflow, CLI and identity-gated recovery integration; eleven backend regressions | Storage and exact Node fixture qualification pass. General native workflow profiles, unbound manual owners and six historical reservations remain unavailable. |

## New operational surfaces

- `GET /api/runs/active` exposes bounded descendant activity, CPU coverage,
  sampled warnings and finite native transport time remaining. Optional
  `childProcessPolicy` accepts `maxChildren`, `maxConcurrentTests` and `action`
  (`warn` by default or `stop`). Sampling cannot prevent a command starting.
- `POST /api/runs/:runId/cancel` requires the exact current request, invocation
  and attempt IDs. MCP exposes `cancel_active_run`. The response acknowledges
  stop intent and explicitly does not assert process-tree death.
- `POST /api/providers/perplexity/probe-answer` requires `confirmQuotaUse:true`
  and the exact expected account. MCP exposes `probe_provider_answer`. It uses
  one fixed prompt through ordinary admission and never retries or falls back.
  Passive readiness remains non-generating. A five-minute observation is scoped
  to account/config generation and does not prove current authentication/quota.
- Delivery receipts link request, invocation, attempt, task and result hash.
  Startup repairs missing receipt metadata from durable results without running
  tasks. Exact result acknowledgement is idempotent. Cached-token amplification
  is advice to review efficiency, never an automatic turn cap or quota estimate.

## Validation checkpoint

The published development checkpoint passed 1,275 tests with six skips (1,281 total).
Subsequent focused checks pass: owner/boundary/queue integration 89, private
backend 11, and terminal authentication/input/remote exclusion 13. A full suite
after the production integration is required before release.
Production dependency audit reported zero vulnerabilities.

Native Windows PowerShell 5.1 tests passed both holder lifecycle scenarios:
an existing holder blocks before npm work, and a holder created during actual
staged npm tests blocks before shutting down the old REST process. Both preserve
the original REST process identity and complete token/data/install tree hashes.

These are development checkpoints. Final review, an exact-head full suite,
Linux/Windows CI and CodeQL must pass before merging and closing eligible rows.

## Candidate and recovery authority

Candidate staging copies actual working bytes, including dirty and untracked
files, and rejects unsupported links/special files and incomplete scans. It
uses pinned directories, full final delta validation and durable evidence.
Any forbidden create/modify/delete/restore quarantines the entire candidate;
allowed changes are not salvaged from a mixed violation. It never applies edits
to the source. `atomicSnapshot:false` is intentional: concurrent host writers
require a separate ownership boundary, and metadata checks are not a multi-file
atomic snapshot. Provider code must not receive access to evidence or another
run's staging directories.

Neither elapsed time, a disappeared PID, a caller boolean, a successful model
answer nor a descendant census can release an expired writer lease. Historical
unbound reservations cannot be upgraded into trusted process ownership after
the fact. The shared running bridge and its other projects remain untouched.


A single explicit native Claude 2.1.258 Read/Write/Edit fixture passed on Linux:
exact staged write, unchanged source/evidence, successful terminal result,
restricted CONNECT transport and complete physical settlement. This qualifies
that experiment only; production native workflow profiles remain unavailable.
See `docs/OWNED-EXECUTION.md` for opt-in, recovery proof and enrollment gaps.

The first Windows CI run passed 1,099 tests and failed one census-observation
wait (179 platform skips). Independent native Windows census samples passed;
the cause of that CI miss was not reproduced. The fixture now records bounded
request/census diagnostics and permits two bounded Windows observation windows.
Exact-head Windows CI remains a required gate.

CodeQL's intentional terminal-command-input finding was reviewed and individually
dispositioned; terminal authority and regression evidence are recorded in
`docs/TERMINAL-SECURITY-REVIEW.md`. No scanner rule or file exclusion was added.
The credential catalog now uses temporary opaque IDs rather than a credential
fingerprint, with exact private identity binding and bounded cache lifetime.
